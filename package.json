{
  "name": "enhanced-email-validation",
  "version": "0.0.10",
  "description": "Validates email format, and checks that the domain belongs to a known email provider",
  "main": "dist/index.js",
  "license": "Unlicense",
  "repository": {
    "type": "git",
    "url": "https://github.com/ian-speers/enhanced-email-validation.git"
  },
  "homepage": "https://github.com/ian-speers/enhanced-email-validation#nhanced-email-validation",
  "scripts": {
    "updateDict": "yarn build && node scripts/updateDict.js",
    "test": "yarn build && jest",
    "build": "rm -rf dist && yarn tsc",
    "publish-npm": "yarn build && npm publish --access public"
  },
  "dependencies": {
    "hunspell-spellchecker": "^1.0.2",
    "lodash": "^4.17.21"
  },
  "devDependencies": {
    "jest": "^29.0.3",
    "typescript": "^4.8.3"
  }
}
