{
  "name": "validate-email",
  "version": "0.0.0",
  "description": "Validates email format, and checks that the domain belongs to a known email provider",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "updateDict": "node scripts/updateDict.js"
  },
  "dependencies": {
    "hunspell-spellchecker": "^1.0.2",
    "jest": "^29.0.3"
  }
}
